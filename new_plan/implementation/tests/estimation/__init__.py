"""Tests for :mod:`afterlap_core.estimation`.

A package rather than a bare directory so ``conftest.py`` here is importable as
``.conftest`` and cannot collide with another module's test fixtures.
"""
