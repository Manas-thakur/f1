"""Tests for ``afterlap_core.planning``.

This directory is a package and its fixtures are imported relatively
(``from .conftest import ...``). ``handoffs/decisions.md`` D-03: a bare
top-level ``conftest.py`` collides with other agents' test directories under
pytest's prepend import mode.
"""
