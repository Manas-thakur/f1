"""Tests for :mod:`afterlap_core.learning`.

This directory is a package and its fixtures are imported relatively, per
``handoffs/decisions.md`` D-03: two sibling test directories each shipping a bare
``conftest.py`` shadow each other under pytest's prepend import mode.
"""
