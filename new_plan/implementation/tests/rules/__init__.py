"""Rules tests.

This package marker exists so that pytest imports ``tests/rules/conftest.py``
as ``rules.conftest`` rather than as the bare module name ``conftest``, which
would collide with another module directory's conftest in ``sys.modules``.
"""
