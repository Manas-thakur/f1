"""Cross-module acceptance tests.

These exercise the closed loop that actually exists today across module
boundaries: A03's simulator, A04's rules and A13's controllers and evaluator.
They are deliberately not unit tests of any one module.
"""
