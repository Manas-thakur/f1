'use strict';
const A=window.Afterlap;
const state={stale:false,stage:'proposed',energy:2.4,gap:.65,opponent:'Defensive',running:false,compared:false,cursor:58,notice:'',events:[]};
const pages=[['engineer','01','Race engineer'],['lab','02','Simulation lab'],['replay','03','Replay & compare'],['evidence','04','Evidence'],['rules','05','Rulesets'],['models','06','Models'],['sessions','07','Sessions'],['settings','08','Preferences']];
const footer=()=>'<footer class="app-footer"><span>ILLUSTRATIVE FIXTURES · NOT MODEL OUTPUT</span><span>Local design preview</span><a href="index.html">All mockups</a></footer>';
function head(title,sub,actions=''){return `<div class="page-head"><div><h1>${title}</h1><p>${sub}</p></div><div class="row">${actions}</div></div>`}
function metrics(){return `<div class="metric-row"><div class="metric"><strong>${state.energy.toFixed(2)} <small>MJ</small></strong><span>Available energy</span><small>SIMULATED FIXTURE</small></div><div class="metric"><strong>${state.gap.toFixed(2)} <small>s</small></strong><span>Gap ahead</span><small>SIMULATED FIXTURE</small></div><div class="metric"><strong>1.20 <small>MJ</small></strong><span>Checkpoint target</span><small>CONFIGURED FIXTURE</small></div></div>`}
function decision(){let text=state.stale?'Advice withdrawn':state.stage==='executing'?'Save through T7':'Save through T7';let description=state.stale?'The fixture feed is stale. The previous instruction is no longer actionable.':'Hold energy through T7. Prepare an attack after T9 with reserve for the following straight.';return `<section class="decision"><div class="row between"><span class="eyebrow">Current recommendation</span><span class="badge ${state.stale?'':'amber'}">${state.stale?'INVALIDATED':state.stage.toUpperCase()}</span></div><h2>${text}</h2><p class="reason">${description}</p>${metrics()}<div class="decision-footer"><span class="small muted">${state.stale?'Restore fixture to resume':'Start: now · End: T7 exit'}</span><div class="row"><button data-action="inspect">View evidence</button><button class="primary" data-action="advance" ${state.stale||state.stage==='executing'?'disabled':''}>${state.stage==='proposed'?'Select plan':state.stage==='selected'?'Mark communicated':state.stage==='communicated'?'Simulate execution':'Executing'}</button></div></div></section>`}
function timeline(){let extra=state.events.map((x,i)=>`<li><time>NOW</time><div><strong>${A.escape(x)}</strong><p>Local interface demonstration</p></div></li>`).join('');return `<ul class="timeline">${extra}<li><time>18:24</time><div><strong>Checkpoint reserve revised</strong><p>Later defence remains part of the decision horizon.</p></div></li><li><time>18:20</time><div><strong>Opponent uncertainty widened</strong><p>Rival energy is estimated; exact charge is unavailable.</p></div></li><li><time>18:16</time><div><strong>Session fixture loaded</strong><p>Test loop · synthetic scenario</p></div></li></ul>`}
function engineer(){return head('Race engineer','Car 01 / Test loop / Energy deployment',`<button data-action="stale">${state.stale?'Restore fixture':'Inject stale feed'}</button>`)+`<div class="content-grid"><div class="stack">${decision()}<section class="panel"><div class="panel-heading"><h2>Battery energy / lap distance</h2><a href="#replay" class="small">Compare from here →</a></div>${A.chart()}${A.legend()}<p class="small muted spaced">Illustrative curves. The full product displays calibrated bands and measured checkpoint outcomes.</p></section></div><div class="stack"><section class="panel"><div class="panel-heading"><h2>Track position</h2><span class="badge">TEST LOOP</span></div>${A.track()}<div class="row between rule-top"><span class="small"><span class="accent">●</span> Car 01</span><span class="small"><span class="aqua">●</span> Rival 02</span><span class="small muted">T9 → T12</span></div></section><section class="panel"><div class="panel-heading"><h2>Battle context</h2><span class="small muted">Fixture</span></div><table class="timing-table"><thead><tr><th>Car</th><th>Gap</th><th>Energy</th></tr></thead><tbody><tr><td>02 / Ahead</td><td>${state.gap.toFixed(2)} s</td><td>Unknown</td></tr><tr class="own"><td>01 / Selected</td><td>—</td><td>${state.energy.toFixed(2)} MJ</td></tr></tbody></table><p class="timing-caption">Gap is relative to Car 01. Rival battery telemetry is unavailable.</p></section><section class="panel"><div class="panel-heading"><h2>Decision log</h2><span class="small muted">Fixture events</span></div>${timeline()}</section></div></div>`}
function lab(){return head('Simulation lab','Scenario configuration and branch comparison',`<button data-action="export">Export fixture</button>`)+`<div class="content-grid"><section class="panel"><div class="panel-heading"><h2>Two-straight battle</h2><span class="badge amber">SYNTHETIC</span></div>${A.track(true)}<div class="row between rule-top"><div><h3>${state.running?'Fixture playback active':'Ready at the decision point'}</h3><p class="small muted">Controls illustrate the future simulation workflow; no physics runs here.</p></div><button class="primary" data-action="run">${state.running?'Pause fixture':'Start fixture'}</button></div><div class="spaced">${A.chart('energy',state.compared)}${state.compared?A.legend():''}</div><div class="row spaced"><button data-action="compare">Compare from here</button><a class="button" href="#replay">Open branch view</a></div>${state.compared?'<p class="notice spaced">Comparison fixture loaded from the same illustrative starting state. These curves are authored examples, not experimental results.</p>':''}</section><section class="panel"><div class="panel-heading"><h2>Scenario parameters</h2><span class="small muted">Local fixture</span></div><div class="field"><label for="energy">Initial energy <output id="energy-out" class="mono">${state.energy.toFixed(2)} MJ</output></label><input id="energy" type="range" min=".8" max="3.8" step=".1" value="${state.energy}"></div><div class="field"><label for="gap">Gap ahead <output id="gap-out" class="mono">${state.gap.toFixed(2)} s</output></label><input id="gap" type="range" min=".1" max="1.5" step=".05" value="${state.gap}"></div><div class="field"><label for="opponent">Opponent policy</label><select id="opponent">${['Defensive','Balanced','Conserving'].map(x=>`<option ${x===state.opponent?'selected':''}>${x}</option>`).join('')}</select></div><div class="field"><label for="source">Observation source</label><select id="source"><option>Simulator observation layer</option></select></div><div class="rule-top stack"><h3>Scenario manifest</h3><p class="small muted mono">Seed 42<br>Test loop · 5.2 km<br>Rule pack: synthetic / unreviewed<br>Hidden rival energy: withheld</p><button data-action="reset">Reset fixture</button></div></section></div>`}
function replay(){return head('Replay & compare','Distance-aligned comparison from a shared snapshot',`<button data-action="export">Export comparison fixture</button>`)+`<div class="notice">Authored comparison fixture. No claimed improvement, causal result or calibrated probability.</div><section class="panel spaced"><div class="row between"><div><h2>Snapshot at T7 approach</h2><p class="muted small">Distance-aligned inspection · <span id="cursor-distance">${Math.round(state.cursor*52).toLocaleString()}</span> m</p></div><span class="badge">SEED 42 / FIXTURE</span></div><div class="field spaced"><label for="cursor">Shared inspection cursor</label><input id="cursor" type="range" min="0" max="100" value="${state.cursor}"></div><div class="split"><div><h3 class="accent">Energy after the attack</h3><div id="energy-chart">${A.chart('energy',true,state.cursor)}</div></div><div><h3 class="aqua">Gap through the response</h3><div id="gap-chart">${A.chart('gap',true,state.cursor)}</div></div></div>${A.legend()}</section><div class="split spaced"><section class="panel stack"><h2>Selected plan</h2><p>Reserve energy for the later opportunity. Inspect whether the position survives the counterattack.</p><span class="badge">ILLUSTRATIVE STRATEGY</span><button data-action="inspect">Inspect decision record</button></section><section class="panel stack"><h2>Reference strategy</h2><p>Spend earlier in the battle. The implementation must let the rival respond to each branch independently.</p><span class="badge">ILLUSTRATIVE STRATEGY</span><a href="#evidence">View required benchmark evidence →</a></section></div>`}
function evidence(){return head('Evidence','Controller benchmarks and evaluation records')+`<section class="panel"><div class="panel-heading"><h2>Benchmark readiness</h2><span class="badge">UNMEASURED</span></div><p class="muted">The interface is ready to show a paired evaluation. No benchmark has been run in this design prototype.</p><div class="table-wrap spaced"><table><thead><tr><th>Controller</th><th>Elapsed time</th><th>Retained passes</th><th>Violations</th><th>p95 latency</th></tr></thead><tbody>${['Legal fixed schedule','Legal greedy attacker','MPC baseline','MPC + learned strategy'].map(x=>`<tr><td>${x}</td><td>—</td><td>—</td><td>—</td><td>—</td></tr>`).join('')}</tbody></table></div></section><div class="split spaced"><section class="panel stack"><h2>What the report must include</h2><p class="muted">Held-out scenarios, paired uncertainty intervals, losses, calibration, hardware, observation access and model hashes.</p><button data-action="report">Inspect report contract</button></section><section class="panel stack"><h2>Promotion gate</h2><p class="muted">The learned contribution remains disabled until it earns promotion against the same planner without RL.</p><span class="badge amber">NOT EVALUATED</span></section></div>`}
function rules(){return head('Rulesets','Applicable constraints, sources and coverage')+`<section class="panel"><div class="panel-heading"><h2>Synthetic event pack</h2><span class="badge amber">REVIEW REQUIRED</span></div><p class="muted">This prototype does not calculate regulatory compliance. The production pack requires reviewed source transcription and boundary tests.</p><div class="table-wrap spaced"><table><thead><tr><th>Constraint family</th><th>Reference</th><th>Implementation status</th></tr></thead><tbody>${[['Electrical power','C5.2.7 / C5.2.8'],['Battery operating range','C5.2.9'],['Recharge ledger','C5.2.10'],['Overtake eligibility','B7.2'],['Driver information path','C8.5 / C8.6']].map(x=>`<tr><td>${x[0]}</td><td class="mono">${x[1]}</td><td>Specification only</td></tr>`).join('')}</tbody></table></div><div class="row spaced"><a class="button" href="https://www.fia.com/regulation/category/2182" target="_blank" rel="noopener">Official FIA documents ↗</a><button data-action="rules">Inspect coverage policy</button></div></section>`}
function models(){return head('Models','Model versions and release status')+`<section class="panel"><div class="table-wrap"><table><thead><tr><th>Component</th><th>Role</th><th>Status</th></tr></thead><tbody><tr><td>Constrained MPC</td><td>Feasible near-term planning</td><td>Planned</td></tr><tr><td>SAC policy</td><td>Segment budget proposal</td><td>Not trained</td></tr><tr><td>Ordinary-return value model</td><td>Long-horizon consequence</td><td>Not trained</td></tr></tbody></table></div></section><section class="panel spaced stack"><h2>Bundle requirements</h2><p class="muted">The approved bundle includes feature order, normaliser, rules family, objective revision, training manifest and held-out benchmark report.</p><div class="row"><button data-action="model">Inspect bundle contract</button><button disabled>Promotion unavailable</button></div><p class="small muted">No candidate or evaluation evidence exists in this prototype.</p></section>`}
function sessions(){return head('Sessions','Session recordings and available integrations')+`<section class="panel"><div class="table-wrap"><table><thead><tr><th>Session</th><th>Source</th><th>Capability</th><th></th></tr></thead><tbody><tr><td>Two-straight counterattack</td><td><span class="badge amber">SYNTHETIC FIXTURE</span></td><td>Interactive design preview</td><td><a href="#engineer">Open →</a></td></tr><tr><td>Public-data replay</td><td>Not imported</td><td>Context analysis</td><td>Unavailable</td></tr><tr><td>Authorised team telemetry</td><td>Not connected</td><td>Requires integration</td><td>Unavailable</td></tr></tbody></table></div></section><p class="notice spaced">Public feeds do not become measured battery telemetry simply by changing the session label.</p>`}
function settings(){return head('Preferences','Interface choices for this local design preview.')+`<section class="panel stack"><h2>Workspace density</h2><p class="muted">Changes spacing only; decision and source labels remain visible.</p><div class="row"><button data-action="comfortable" aria-pressed="${!document.body.classList.contains('compact')}">Comfortable</button><button data-action="compact" aria-pressed="${document.body.classList.contains('compact')}">Compact</button></div><div class="rule-top stack"><h2>Motion and accessibility</h2><p class="muted">The prototype respects the operating system’s reduced-motion preference and includes visible keyboard focus. It does not auto-advance charts.</p></div><div class="rule-top stack"><h2>Fixture communication</h2><p class="muted">The driver display can observe local demonstration state on the same browser origin. This is not a production telemetry link.</p><button data-action="clear">Clear local driver instruction</button></div></section>`}
function render(){let route=location.hash.slice(1)||'engineer';if(!pages.some(x=>x[0]===route))route='engineer';document.getElementById('navigation').innerHTML=pages.map(([id,s,n])=>`<a href="#${id}" ${id===route?'aria-current="page"':''}>${n}</a>`).join('');document.getElementById('content').innerHTML=({engineer,lab,replay,evidence,rules,models,sessions,settings}[route])()+footer();document.getElementById('quality-status').innerHTML=state.stale?'<i class="status-dot bad"></i>Stale fixture · advice withdrawn':'<i class="status-dot"></i>Fixture available';bind();}
function publish(){try{localStorage.setItem('afterlap-design-driver',JSON.stringify({stage:state.stage,stale:state.stale,energy:state.energy,instruction:'SAVE',until:'UNTIL T7',updatedAt:Date.now(),expiresAt:Date.now()+30000,provenance:'illustrative fixture'}))}catch(e){state.notice='Local storage unavailable; open the driver controls directly.'}}
function inspect(type='inspect'){const content={inspect:`<h2 id="inspector-title">Decision record</h2><span class="badge amber">ILLUSTRATIVE FIXTURE</span><p><strong>Reason:</strong> preserve electrical assistance for the response after the first attack.</p><p><strong>Inputs:</strong> own energy ${state.energy.toFixed(2)} MJ and gap ${state.gap.toFixed(2)} s are simulated fixtures. Rival charge is unknown.</p><p><strong>Prediction:</strong> chart shapes are authored design examples. Pass probability and measured benefit are unavailable.</p><p><strong>Rule result:</strong> not evaluated. Production advice requires the independent checker.</p><p><strong>Lifecycle:</strong> ${state.stale?'invalidated':state.stage}. Selection and execution are separate events.</p>`,report:'<h2 id="inspector-title">Report contract</h2><p>Scenario manifest · source revision · seed population · controller observations · physical outcomes · confidence intervals · all failures · calibration · hardware and latency.</p><p>No measured report is available in this prototype.</p>',rules:'<h2 id="inspector-title">Coverage policy</h2><p>Each applicable check returns pass, fail or unknown with source article, rule-pack hash and margin. Missing supporting conditions remain unknown. A generic green “FIA compliant” badge is not supported.</p>',model:'<h2 id="inspector-title">Model bundle</h2><p>Weights hash · feature schema · normaliser · rule family · objective revision · training manifest · evaluation report · operator approval.</p><p>Runtime weights remain frozen. The learned contribution cannot promote itself.</p>'};document.getElementById('inspector-body').innerHTML=content[type]||content.inspect;document.getElementById('inspector').showModal();}
function bind(){document.querySelectorAll('[data-action]').forEach(b=>b.addEventListener('click',()=>{let act=b.dataset.action;if(['inspect','report','rules','model'].includes(act)){inspect(act);return}if(act==='stale'){state.stale=!state.stale;state.stage='proposed';state.events.unshift(state.stale?'Advice invalidated: stale data':'Fixture restored; new proposal');publish()}if(act==='advance'){state.stage=({proposed:'selected',selected:'communicated',communicated:'executing'})[state.stage]||state.stage;state.events.unshift(({selected:'Engineer selected the plan',communicated:'Engineer marked instruction communicated',executing:'Driver execution simulated'})[state.stage]);publish()}if(act==='run')state.running=!state.running;if(act==='compare'){state.compared=true;state.events.unshift('Comparison fixture opened')}if(act==='reset'){state.energy=2.4;state.gap=.65;state.opponent='Defensive';state.running=false;state.compared=false}if(act==='export'){A.download('afterlap-illustrative-fixture.json',{notice:'Illustrative design fixture. Not telemetry, physics or benchmark evidence.',energy_mj:state.energy,gap_s:state.gap,opponent:state.opponent,seed:42});return}if(act==='compact')document.body.classList.add('compact');if(act==='comfortable')document.body.classList.remove('compact');if(act==='clear'){state.stale=true;publish();state.stale=false;state.stage='proposed';state.events.unshift('Local driver instruction cleared')}state.events=state.events.slice(0,3);render()}));let e=document.getElementById('energy');if(e)e.addEventListener('input',()=>{state.energy=Number(e.value);document.getElementById('energy-out').textContent=state.energy.toFixed(2)+' MJ'});let g=document.getElementById('gap');if(g)g.addEventListener('input',()=>{state.gap=Number(g.value);document.getElementById('gap-out').textContent=state.gap.toFixed(2)+' s'});let o=document.getElementById('opponent');if(o)o.addEventListener('change',()=>state.opponent=o.value);let c=document.getElementById('cursor');if(c)c.addEventListener('input',()=>{state.cursor=Number(c.value);document.getElementById('cursor-distance').textContent=Math.round(state.cursor*52).toLocaleString();document.getElementById('energy-chart').innerHTML=A.chart('energy',true,state.cursor);document.getElementById('gap-chart').innerHTML=A.chart('gap',true,state.cursor)})}
document.querySelector('.dialog-close').addEventListener('click',()=>document.getElementById('inspector').close());window.addEventListener('hashchange',render);render();
